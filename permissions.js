module.exports= 
{
    execute(oldChannel, newChannel)
    {
        var guestPrefix = "[G]";
        var trialPrefix = "[T]";
        var memberPrefix = "[M]";
        var vetPrefix = "[V]";
        var modPrefix = "[Mod]";
        var modLeaderPrefix = "[ML]";
        if (newChannel.name.includes(guestPrefix))
        {
            // do nothing
            return;
        }
        return;
    },   
};      